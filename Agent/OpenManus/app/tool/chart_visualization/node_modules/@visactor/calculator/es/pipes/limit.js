export const limit = ({ limit: limitSize }) => {
    return (tableData) => {
        if (limitSize === undefined
            || limitSize < 1) {
            return tableData;
        }
        return tableData.slice(0, limitSize);
    };
};
