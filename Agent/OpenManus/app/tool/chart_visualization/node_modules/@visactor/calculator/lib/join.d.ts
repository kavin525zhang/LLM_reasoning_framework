import { type TableData, JoinType, type ColumnName } from './types';
export declare const join: ({ type, left, right, using }: {
    type: JoinType;
    left: TableData;
    right: TableData;
    using: ColumnName[];
}) => TableData;
export declare const leftJoin: ({ left, right, using }: {
    left: TableData;
    right: TableData;
    using: ColumnName[];
}) => TableData;
export declare const rightJoin: ({ left, right, using }: {
    left: TableData;
    right: TableData;
    using: ColumnName[];
}) => TableData;
export declare const innerJoin: ({ left, right, using }: {
    left: TableData;
    right: TableData;
    using?: ColumnName[];
}) => TableData;
export declare const crossJoin: ({ left, right }: {
    left: TableData;
    right: TableData;
}) => TableData;
