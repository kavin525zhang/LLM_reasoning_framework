import type { ColumnName, ColumnEvaluate, TableData, GroupMap, GroupedData } from '../types';
export type GroupPipe = (params: {
    groupBy?: (ColumnName | ColumnEvaluate)[];
    source: TableData;
}) => ((tableData: TableData) => GroupedData);
export declare const group: GroupPipe;
export declare const buildGroupsMap: ({ tableData, groupBy, source }: {
    tableData: TableData;
    groupBy: (ColumnName | ColumnEvaluate)[];
    source?: TableData;
}) => GroupMap;
