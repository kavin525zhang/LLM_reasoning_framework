import { judgeFilterTree } from './filter';
import { evaluateRowColumnValue, } from '../select';
export const where = ({ filter, source }) => {
    if (!(filter === null || filter === void 0 ? void 0 : filter.conditions.length))
        return (tableData) => tableData;
    return (tableData) => tableData.filter(row => {
        return judgeFilterTree({
            filterNode: filter,
            item: row,
            getValue: ({ item, condition }) => evaluateRowColumnValue({
                row: item,
                source,
                column: condition
            }),
        });
    });
};
