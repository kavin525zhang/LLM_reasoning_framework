"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.min = exports.max = exports.avg = exports.sum = exports.count = void 0;
const count = ({ values }) => {
    return values.length;
};
exports.count = count;
const sum = ({ values }) => {
    if (!values.length) {
        return null;
    }
    return values.reduce((total, current) => total + ((typeof current === 'number' && isFinite(current))
        ? current
        : 0), 0);
};
exports.sum = sum;
const avg = ({ column, values }) => {
    if (!values.length) {
        return null;
    }
    const total = (0, exports.sum)({ column, values });
    return total / values.length;
};
exports.avg = avg;
const max = ({ values }) => {
    if (!values.length) {
        return null;
    }
    let maximum = null;
    values.forEach(value => {
        if (maximum === null
            || (value !== null && value > maximum)) {
            maximum = value;
        }
    });
    return maximum;
};
exports.max = max;
const min = ({ values }) => {
    if (!values.length) {
        return null;
    }
    let minimum = null;
    values.forEach(value => {
        if (minimum === null
            || (value !== null && value < minimum)) {
            minimum = value;
        }
    });
    return minimum;
};
exports.min = min;
