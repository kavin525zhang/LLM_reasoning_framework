import type { TableData, FilterNode, WhereCondition } from '../../types';
export type WherePipe = (params: {
    filter?: FilterNode<WhereCondition>;
    source: TableData;
}) => ((tableData: TableData) => TableData);
export declare const where: WherePipe;
