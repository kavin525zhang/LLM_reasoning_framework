"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.query = void 0;
const pipe_1 = require("./pipe");
const pipes = __importStar(require("./pipes"));
const query = (query) => {
    const { from, select, where, groupBy, having, orderBy, limit } = query;
    if (!from.length)
        return [];
    const needGroup = Boolean((groupBy === null || groupBy === void 0 ? void 0 : groupBy.length) || select.columns.some(column => 'aggregate' in column && column.aggregate));
    if (needGroup) {
        return (0, pipe_1.of)(from).pipe(pipes.where({ filter: where, source: from }), pipes.group({ groupBy, source: from }), pipes.having({ filter: having, source: from }), pipes.orderGroup({ orderBy, source: from }), pipes.selectGroup({ select, source: from }), pipes.distinct({ select }), pipes.limit({ limit }));
    }
    return (0, pipe_1.of)(from).pipe(pipes.where({ filter: where, source: from }), pipes.order({ orderBy, source: from }), pipes.select({ select, source: from }), pipes.distinct({ select }), pipes.limit({ limit }));
};
exports.query = query;
