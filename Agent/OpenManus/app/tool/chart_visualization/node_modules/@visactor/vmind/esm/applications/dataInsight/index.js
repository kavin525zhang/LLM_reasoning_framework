import { AtomName } from "../../types";

import { Schedule } from "../../schedule";

export const getDataInsightSchedule = (llm, options) => new Schedule([ AtomName.DATA_INSIGHT, AtomName.SPEC_INSIGHT ], {
    base: {
        llm: llm,
        showThoughts: null == options ? void 0 : options.showThoughts
    }
});