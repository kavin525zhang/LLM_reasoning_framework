import { Schedule } from "../../schedule";

import { AtomName, Model } from "../../types";

export const getData2ChartSchedule = (llm, options) => {
    var _a, _b;
    return !((null === (_a = llm.options.customRequestFunc) || void 0 === _a ? void 0 : _a.chartGenerate) || llm.options.model !== Model.CHART_ADVISOR && (null === (_b = llm.options) || void 0 === _b ? void 0 : _b.headers)) ? new Schedule([ AtomName.CHART_GENERATE ], {
        base: {
            llm: llm
        },
        chartGenerate: {
            useChartAdvisor: !0
        }
    }) : new Schedule([ AtomName.DATA_QUERY, AtomName.CHART_COMMAND, AtomName.CHART_GENERATE ], {
        base: {
            llm: llm,
            showThoughts: null == options ? void 0 : options.showThoughts
        },
        chartCommand: {
            useDataTable: !0
        }
    });
};
//# sourceMappingURL=index.js.map