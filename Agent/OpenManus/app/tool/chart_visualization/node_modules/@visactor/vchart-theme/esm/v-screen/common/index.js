import { markByName } from "./mark";

import { axis } from "./component/axis";

import { crosshair } from "./component/crosshair";

import { tooltip } from "./component/tooltip";

import { getColorScheme } from "./color-scheme";

import { bar } from "./series/bar";

import { area } from "./series/area";

export const getVScreenCommonTheme = (name, description, dataScheme) => ({
    name: name,
    description: description,
    type: "dark",
    colorScheme: getColorScheme(dataScheme),
    markByName: markByName,
    series: Object.assign(Object.assign({}, bar), area),
    component: Object.assign(Object.assign({}, axis), {
        crosshair: crosshair,
        tooltip: tooltip
    })
})
//# sourceMappingURL=index.js.map
;