import { palette } from "./palette";

export const getColorScheme = dataScheme => ({
    default: {
        dataScheme: dataScheme,
        palette: palette
    }
})
//# sourceMappingURL=color-scheme.js.map
;